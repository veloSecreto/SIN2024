#include "asset_manager.h"

namespace AssetManager {
    GLuint _vao, _vbo, _ebo;

    void loadAll() {
        loadForTesting();
    }

    void loadForTesting() {
        std::vector<GLfloat> vertices = {
            
        };
    }
};